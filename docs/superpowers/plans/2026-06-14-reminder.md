# 倒计时提醒功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为待办卡片添加右键菜单，支持设置倒计时提醒，到期后弹出系统通知。

**Architecture:** 新增 `ReminderService` 管理提醒定时器，使用 Obsidian `Menu` API 创建右键菜单，浏览器 Notification API 弹出系统通知。仅当前会话有效，不持久化。

**Tech Stack:** Obsidian Plugin API (Menu, Notice, PromptModal), 浏览器 Notification API, 现有 TimerManager

---

## 文件结构

| 操作 | 文件 | 职责 |
|------|------|------|
| 新增 | `services/ReminderService.js` | 提醒定时器管理、系统通知触发 |
| 修改 | `plugin.js:15-16,85-86,333-355` | 实例化/清理 ReminderService，删除任务时取消提醒 |
| 修改 | `views/TodoView.js:221-236,819-920` | 右键菜单事件委托、卡片闹钟图标 |
| 修改 | `styles.css:314-319` | 闹钟图标样式 |

---

### Task 1: 创建 ReminderService

**Files:**
- Create: `services/ReminderService.js`

- [ ] **Step 1: 创建 ReminderService.js**

```javascript
const { Notice } = require('obsidian');

class ReminderService {
  constructor(timerManager, app) {
    this.timerManager = timerManager;
    this.app = app;
    this.reminders = new Map(); // taskId -> { timerId, fireAt, content }
    this._requestPermission();
  }

  // 静默请求通知权限
  _requestPermission() {
    if (typeof Notification === 'undefined') return;
    if (Notification.permission === 'default') {
      Notification.requestPermission();
    }
  }

  // 设置提醒，返回 fireAt 时间戳
  setReminder(taskId, content, delayMs) {
    // 如果已有提醒，先取消
    this.cancelReminder(taskId);

    const fireAt = Date.now() + delayMs;
    const timerId = this.timerManager.setTimeout(() => {
      this._notify(taskId, content);
      this.reminders.delete(taskId);
      // 通知视图刷新闹钟图标
      this._notifyViewsToRefresh();
    }, delayMs);

    this.reminders.set(taskId, { timerId, fireAt, content });
    return fireAt;
  }

  // 取消提醒
  cancelReminder(taskId) {
    const reminder = this.reminders.get(taskId);
    if (!reminder) return false;
    // TimerManager 的 setTimeout 返回原生 timer ID，用 clearTimeout 取消
    clearTimeout(reminder.timerId);
    this.reminders.delete(taskId);
    return true;
  }

  // 查询是否有提醒
  hasReminder(taskId) {
    return this.reminders.has(taskId);
  }

  // 返回剩余毫秒数
  getRemainingTime(taskId) {
    const reminder = this.reminders.get(taskId);
    if (!reminder) return 0;
    return Math.max(0, reminder.fireAt - Date.now());
  }

  // 清除所有提醒
  clearAll() {
    for (const reminder of this.reminders.values()) {
      clearTimeout(reminder.timerId);
    }
    this.reminders.clear();
  }

  // 触发通知
  _notify(taskId, content) {
    if (typeof Notification !== 'undefined' && Notification.permission === 'granted') {
      const notification = new Notification('待办提醒', {
        body: content,
        icon: undefined
      });
      notification.onclick = () => {
        window.focus();
        notification.close();
      };
    } else {
      // fallback 到 Obsidian Notice
      new Notice(`⏰ 提醒: ${content}`, 10000);
    }
  }

  // 通知视图刷新
  _notifyViewsToRefresh() {
    const leaves = this.app.workspace.getLeavesOfType('todo-kanban-view');
    for (const leaf of leaves) {
      const view = leaf.view;
      if (view && view.renderTasks) {
        view.renderTasks();
      }
    }
  }
}

module.exports = ReminderService;
```

- [ ] **Step 2: 验证文件语法正确**

Run: `node -c services/ReminderService.js`
Expected: 无输出（语法正确）

- [ ] **Step 3: Commit**

```bash
git add services/ReminderService.js
git commit -m "feat: add ReminderService for countdown reminders"
```

---

### Task 2: 在 plugin.js 中集成 ReminderService

**Files:**
- Modify: `plugin.js:4-6,15-16,85-86,333-355`

- [ ] **Step 1: 添加 import**

在 `plugin.js` 第 6 行后添加：

```javascript
const ReminderService = require('./services/ReminderService');
```

- [ ] **Step 2: 在构造函数中实例化 ReminderService**

在 `plugin.js` 构造函数中，`this.errorHandler` 之后添加：

```javascript
this.reminderService = null; // onload 中初始化
```

- [ ] **Step 3: 在 onload 中初始化 ReminderService**

在 `plugin.js` 的 `onload()` 方法中，`await this.loadTasks()` 之后添加：

```javascript
this.reminderService = new ReminderService(this.timerManager, this.app);
```

- [ ] **Step 4: 在 onunload 中清理提醒**

在 `plugin.js` 的 `onunload()` 方法中，`this.timerManager.clearAll()` 之后添加：

```javascript
if (this.reminderService) {
  this.reminderService.clearAll();
}
```

- [ ] **Step 5: 在 deleteTask 中取消提醒**

在 `plugin.js` 的 `deleteTask()` 方法中，`dateTask.tasksList.splice(taskIndex, 1)` 之后、`if (dateTask.tasksList.length === 0)` 之前添加：

```javascript
// 取消该任务的提醒
if (this.reminderService) {
  this.reminderService.cancelReminder(taskId);
}
```

- [ ] **Step 6: 验证语法**

Run: `node -c plugin.js`
Expected: 无输出

- [ ] **Step 7: Commit**

```bash
git add plugin.js
git commit -m "feat: integrate ReminderService in plugin lifecycle"
```

---

### Task 3: 添加右键菜单到 TodoView

**Files:**
- Modify: `views/TodoView.js:1,221-236`

- [ ] **Step 1: 添加 Menu 到 import**

修改 `views/TodoView.js` 第 1 行，将：

```javascript
const { ItemView, Notice } = require('obsidian');
```

改为：

```javascript
const { ItemView, Notice, Menu, PromptModal } = require('obsidian');
```

- [ ] **Step 2: 在 setupEventDelegation 中添加 contextmenu 事件**

在 `views/TodoView.js` 的 `setupEventDelegation` 方法中，在现有的 `click` 事件监听之后（`}, { signal });` 之后、`// 注：拖拽排序事件` 注释之前），添加：

```javascript
// 右键菜单委托
this.todoContainer.addEventListener('contextmenu', (e) => {
  const card = e.target.closest('.todo-card');
  if (!card) return;
  e.preventDefault();
  const taskId = card.dataset.taskId;
  this.showReminderMenu(e, taskId);
}, { signal });
```

- [ ] **Step 3: 添加 showReminderMenu 方法**

在 `views/TodoView.js` 中，在 `showExportDialog()` 方法之前（约第 742 行），添加：

```javascript
// 显示提醒右键菜单
showReminderMenu(e, taskId) {
  const menu = new Menu();
  const reminderService = this.plugin.reminderService;
  const task = this.plugin.findTaskById(taskId);
  if (!task || !reminderService) return;

  const presets = [
    { label: '10 分钟后', ms: 10 * 60 * 1000 },
    { label: '30 分钟后', ms: 30 * 60 * 1000 },
    { label: '1 小时后', ms: 60 * 60 * 1000 },
    { label: '3 小时后', ms: 3 * 60 * 60 * 1000 },
  ];

  menu.addItem(item => item.setTitle('📋 提醒我').setDisabled(true));

  presets.forEach(preset => {
    menu.addItem(item => item
      .setTitle(preset.label)
      .onClick(() => {
        reminderService.setReminder(taskId, task.content, preset.ms);
        new Notice(`已设置 ${preset.label} 提醒`, 3000);
        this.renderTasks();
      })
    );
  });

  menu.addItem(item => item
    .setTitle('自定义时间...')
    .onClick(() => {
      new PromptModal(
        this.app,
        '设置提醒时间',
        '请输入分钟数（1-1440）',
        (value) => {
          const mins = parseInt(value, 10);
          if (!value || isNaN(mins) || mins < 1 || mins > 1440) {
            new Notice('请输入 1-1440 之间的正整数', 3000);
            return;
          }
          reminderService.setReminder(taskId, task.content, mins * 60 * 1000);
          new Notice(`已设置 ${mins} 分钟后提醒`, 3000);
          this.renderTasks();
        }
      ).open();
    })
  );

  if (reminderService.hasReminder(taskId)) {
    menu.addSeparator();
    const remaining = reminderService.getRemainingTime(taskId);
    const mins = Math.ceil(remaining / 60000);
    menu.addItem(item => item
      .setTitle(`⏰ 取消提醒 (剩余 ${mins} 分钟)`)
      .onClick(() => {
        reminderService.cancelReminder(taskId);
        new Notice('已取消提醒', 3000);
        this.renderTasks();
      })
    );
  }

  menu.showAtPosition({ x: e.clientX, y: e.clientY });
}
```

- [ ] **Step 4: 验证语法**

Run: `node -c views/TodoView.js`
Expected: 无输出

- [ ] **Step 5: Commit**

```bash
git add views/TodoView.js
git commit -m "feat: add right-click reminder menu to task cards"
```

---

### Task 4: 添加闹钟图标到任务卡片

**Files:**
- Modify: `views/TodoView.js:293-357,819-920`

- [ ] **Step 1: 在 updateCardContent 中添加闹钟图标**

在 `views/TodoView.js` 的 `updateCardContent` 方法中，找到最后的 `if (task.link)` 块之后、方法结束之前，添加：

```javascript
// 闹钟图标
const actions = card.querySelector('.todo-actions');
if (actions) {
  const existingIcon = actions.querySelector('.todo-reminder-icon');
  if (this.plugin.reminderService?.hasReminder(task.taskId)) {
    if (!existingIcon) {
      const remaining = this.plugin.reminderService.getRemainingTime(task.taskId);
      const mins = Math.ceil(remaining / 60000);
      const alarmIcon = createEl('span', {
        cls: 'todo-reminder-icon',
        text: '⏰',
        attr: { 'data-task-id': task.taskId, title: `${mins} 分钟后提醒` }
      });
      actions.insertBefore(alarmIcon, actions.firstChild);
    }
  } else if (existingIcon) {
    existingIcon.remove();
  }
}
```

- [ ] **Step 2: 在 createTaskCard 中添加闹钟图标**

在 `views/TodoView.js` 的 `createTaskCard` 方法中，找到 `const actions = card.createEl('div', { cls: 'todo-actions' });` 之后、`const deleteBtn = actions.createEl(...)` 之前，添加：

```javascript
// 闹钟图标（仅当有提醒时）
if (this.plugin.reminderService?.hasReminder(task.taskId)) {
  const remaining = this.plugin.reminderService.getRemainingTime(task.taskId);
  const mins = Math.ceil(remaining / 60000);
  actions.createEl('span', {
    cls: 'todo-reminder-icon',
    text: '⏰',
    attr: { 'data-task-id': task.taskId, title: `${mins} 分钟后提醒` }
  });
}
```

- [ ] **Step 3: 验证语法**

Run: `node -c views/TodoView.js`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add views/TodoView.js
git commit -m "feat: show alarm icon on cards with active reminders"
```

---

### Task 5: 添加 CSS 样式

**Files:**
- Modify: `styles.css:314-319`

- [ ] **Step 1: 添加闹钟图标样式**

在 `styles.css` 的 `.todo-actions` 样式块之后（第 319 行后），添加：

```css
.todo-reminder-icon {
  cursor: default;
  font-size: 12px;
  opacity: 0.7;
  margin-right: 4px;
}

.todo-reminder-icon:hover {
  opacity: 1;
}
```

- [ ] **Step 2: Commit**

```bash
git add styles.css
git commit -m "feat: add reminder icon CSS styles"
```

---

### Task 6: 构建验证

**Files:** None

- [ ] **Step 1: 运行 tsc 类型检查**

Run: `npm run build`
Expected: 无错误输出

- [ ] **Step 2: 运行 esbuild 打包**

Run: `node esbuild.config.mjs production`
Expected: 成功生成 `build/main.js`

- [ ] **Step 3: Commit（如有修复）**

如果有构建错误，修复后提交：

```bash
git add -A
git commit -m "fix: resolve build errors for reminder feature"
```

---

### Task 7: PromptModal API 验证与修正

Obsidian 的 `PromptModal` 构造函数签名可能因版本不同而变化。如果 Task 3 中的 `PromptModal` 调用报错，需要调整。

**Files:**
- Modify: `views/TodoView.js`（仅在 Task 3 的 PromptModal 调用报错时）

- [ ] **Step 1: 检查 PromptModal 可用性**

在 Obsidian 中，`PromptModal` 可能不是公开 API。如果导入失败，使用自定义 Modal 替代。

在 `views/TodoView.js` 中，将 `PromptModal` 相关代码替换为内联 Modal：

```javascript
// 将 PromptModal 调用替换为自定义实现
menu.addItem(item => item
  .setTitle('自定义时间...')
  .onClick(() => {
    const modal = new Modal(this.app);
    modal.titleEl.setText('设置提醒时间');

    const inputEl = modal.contentEl.createEl('input', {
      type: 'number',
      placeholder: '请输入分钟数（1-1440）',
      attr: { min: '1', max: '1440' }
    });
    inputEl.style.width = '100%';
    inputEl.style.padding = '8px';
    inputEl.style.marginTop = '8px';
    inputEl.style.marginBottom = '8px';

    const btnContainer = modal.contentEl.createEl('div');
    btnContainer.style.display = 'flex';
    btnContainer.style.gap = '8px';
    btnContainer.style.justifyContent = 'flex-end';

    const cancelBtn = btnContainer.createEl('button', { text: '取消' });
    cancelBtn.addEventListener('click', () => modal.close());

    const confirmBtn = btnContainer.createEl('button', { text: '确定' });
    confirmBtn.style.backgroundColor = 'var(--interactive-accent)';
    confirmBtn.style.color = 'white';
    confirmBtn.style.border = 'none';
    confirmBtn.style.padding = '6px 16px';
    confirmBtn.style.borderRadius = '4px';
    confirmBtn.addEventListener('click', () => {
      const mins = parseInt(inputEl.value, 10);
      if (!inputEl.value || isNaN(mins) || mins < 1 || mins > 1440) {
        new Notice('请输入 1-1440 之间的正整数', 3000);
        return;
      }
      reminderService.setReminder(taskId, task.content, mins * 60 * 1000);
      new Notice(`已设置 ${mins} 分钟后提醒`, 3000);
      this.renderTasks();
      modal.close();
    });

    inputEl.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') confirmBtn.click();
    });

    modal.open();
    inputEl.focus();
  })
);
```

同时更新 import 行，移除 `PromptModal`：

```javascript
const { ItemView, Notice, Menu, Modal } = require('obsidian');
```

- [ ] **Step 2: 验证构建**

Run: `node esbuild.config.mjs production`
Expected: 成功

- [ ] **Step 3: Commit（仅在修改时）**

```bash
git add views/TodoView.js
git commit -m "fix: replace PromptModal with custom Modal for time input"
```
